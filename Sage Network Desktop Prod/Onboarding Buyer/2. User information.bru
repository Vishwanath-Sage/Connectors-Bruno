meta {
  name: 2. User information
  type: http
  seq: 1
}

get {
  url: {{sageIdURL}}/userinfo
  body: none
  auth: inherit
}

script:post-response {
  var jsonData = res.getBody();
  bru.setEnvVar("provisionAccountEmailBuyer", jsonData.email);
  bru.setVar("provisionAccountEmailBuyer", jsonData.email);
}

settings {
  encodeUrl: true
}
