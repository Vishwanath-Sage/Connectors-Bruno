meta {
  name: Delay for 1 min to send payment
  type: http
  seq: 11
}

get {
  url: https://postman-echo.com/get
  body: none
  auth: inherit
}

script:post-response {
  const delay = ms => new Promise(res => setTimeout(res, ms));
   
  (async () => {
      await delay(60000); // 60 seconds
      bru.setNextRequest("Send Payment-Buyer");
  })();
}

settings {
  encodeUrl: true
}
