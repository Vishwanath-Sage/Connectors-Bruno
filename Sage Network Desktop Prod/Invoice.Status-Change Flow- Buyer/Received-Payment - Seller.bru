meta {
  name: Received-Payment - Seller
  type: http
  seq: 17
}

patch {
  url: {{sageNetworkBaseUrl}}/api/v1/Invoices/{{Seller_invoiceId}}
  body: json
  auth: inherit
}

headers {
  accept: application/vnd.api+json
  X-Group-Key: {{networkProductInstancesTenantIdSeller}}
  Authorization: Bearer {{m2m_jwt_connector}}
}

body:json {
  {
          "workflowStatusId": "00000000-0000-0000-0000-110000000001"
  }
  
}

script:post-response {
  test("Response status code is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
  
}

settings {
  encodeUrl: true
}
