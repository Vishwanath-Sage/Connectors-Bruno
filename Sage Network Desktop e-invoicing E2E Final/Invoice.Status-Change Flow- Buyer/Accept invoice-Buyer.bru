meta {
  name: Accept invoice-Buyer
  type: http
  seq: 6
}

patch {
  url: {{sageNetworkBaseUrl}}/api/v1/Invoices/{{Buyer_invoiceId}}
  body: json
  auth: inherit
}

headers {
  accept: application/vnd.api+json
  X-Group-Key: {{networkProductInstancesTenantIdBuyer}}
  Authorization: Bearer {{m2m_jwt_connector}}
}

body:json {
  {
          "workflowStatusId": "00000000-0000-0000-0000-400000000000"
  }
}

script:post-response {
  test("Status code 200", function() {
      expect(res.getStatus()).to.equal(200);
  });
  
  // Test to check if targetworkflowStatusId and responseworkflowStatusId are the same
  // // pm.test("Target workflowStatusId matches response workflowStatusId", function() {
  //    const targetworkflowStatusId = "00000000-0000-0000-0000-400000000000";
  // // const responseworkflowStatusId = pm.response.json().records[0].workflowStatusId; // Get the current response status
  
  // //  pm.expect(targetworkflowStatusId).to.equal(responseworkflowStatusId);
  // });
  
}

settings {
  encodeUrl: true
}
