meta {
  name: Delay for 1 min to approve invoice
  type: http
  seq: 8
}

get {
  url: https://postman-echo.com/get
  body: none
  auth: inherit
}

script:post-response {
  const delay = ms => new Promise(res => setTimeout(res, ms));
   
  (async () => {
      await delay(60000); // 60 seconds
      bru.setNextRequest("Approve invoice-Buyer");
  })();
}

settings {
  encodeUrl: true
}
